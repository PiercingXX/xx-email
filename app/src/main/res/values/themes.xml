<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose draws everything; this is only the pre-render window background. -->
    <style name="Theme.XxEmail" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
